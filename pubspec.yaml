name: wog_app
description: "Word of God - Integrated App"
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  xml: ^6.5.0
  google_fonts: ^6.2.1
  share_plus: ^10.1.0 # Sharing కోసం
  path_provider: ^2.1.2 # Bookmarks/Offline storage కోసం

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/bible.xml
